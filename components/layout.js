// components/Layout.js
import Head from 'next/head';

const Layout = ({  title }) => {
  return (
    <div>
    piqnepi pw tpw tp iwetpiw tpi wetiwe tpw etpwi tw 
    {title}
    </div>
  );
};

export default Layout;
